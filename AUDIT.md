# 严格审计记录

审计对象：`skills/vps-proxy-node-setup`
最近审计：2026-07-31
审计方式：逐文件源码检查、离线测试、官方发布资产校验，以及全新 Debian 12 测试 VPS 的两次真实部署验证。

## 已通过

- `SKILL.md` 元数据通过 Codex skill validator。
- `bash -n` 通过全部 shell 脚本。
- JSON 模板与固定版本清单通过 JSON 解析。
- 订阅服务模板通过 Python 编译检查。
- sing-box `v1.13.12` 的 amd64/arm64 压缩包已从官方 GitHub Release 下载并重新计算 SHA-256，与清单一致。
- 默认网络参数未包含 BBRv3 虚假 sysctl、MSS Clamp、MTU 修改、IPv4 优先或激进队列参数。
- 端口跳跃只创建和清理自有 NAT 链，不清空共享 `PREROUTING`。
- 回滚按“先删除自有文件/规则、后恢复备份”的顺序执行，并保存 UFW 原始启用状态与默认策略。
- SSH 端口、root 登录策略、密码登录策略均不被脚本修改。
- 真实 VPS 部署已通过：依赖安装、BBR 启用、sing-box 校验、Reality/Hysteria2 服务、订阅服务、端口跳跃 NAT 链和 `postcheck`。
- 已对真实部署执行一次 `resume`；其未重复安装、未重生成凭据，三个服务保持 active。
- 第二次重置系统后，全流程没有再出现依赖顺序或脚本权限问题，外部订阅访问返回 HTTP 200。

## 本次真实部署中发现并修复

- 全新系统缺少 `jq` 时，旧顺序会在安装依赖前读取版本清单并停止；已改为先安装和校验依赖，再读取清单。
- 端口跳跃脚本受到安全 umask 影响而不可执行；已显式设置为 `0755`，并通过 `resume` 从失败处恢复成功。
- 订阅令牌原先会出现在 systemd 启动参数中；已改为读取 `0600` 权限的令牌文件。
- 部署后检查原先只需匹配任意一种节点链接；已改为分别验证 VLESS 与 Hysteria2，并检查 DNAT 跳转和转发规则。
- Fail2ban 原先可能误封执行 Agent 的 SSH 来源地址；已加入当前 SSH 来源白名单并缩短封禁时间。
- 回滚原先依据清单执行递归删除；已改为非递归删除和固定路径白名单，并拒绝异常备份路径。
- 原先缺少 OS 初始化、RAM+swap、剩余磁盘、端口占用和既有 NAT/UFW 冲突检查；已补齐。

## 必须知道的边界

- 本轮安全加固后的修订已通过离线测试，但尚未在第三台重置 VPS 上再次前向测试；交付包专门用于这一独立复测。
- 尚未用独立的外部客户端完成 Reality/Hysteria2 真实代理握手；这仍受云安全组、外部网络和客户端实现影响。
- 默认订阅地址是令牌保护的 HTTP，不是 HTTPS。没有域名和证书时这是有意的边界，不应把订阅地址公开或写入日志。
- 云厂商安全组不在 SSH 脚本控制范围内，必须允许 TCP 443、UDP 8443、UDP 2500–3600 和生成的订阅 TCP 端口。

## 发布门槛

在补完一次真实 `rollback`、本轮修订的独立 Agent 前向测试，以及外部客户端的 Reality/Hysteria2 真实握手后，才可以打 GitHub 发布标签。
